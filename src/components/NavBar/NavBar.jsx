import React from "react";

import {
  StyledNavBar,
  NavContainer,
} from "../styled/StyledNavbar/StyledNavBar";

const NavBar = () => {
  return (
    <StyledNavBar>
      <NavContainer>loooll</NavContainer>
    </StyledNavBar>
  );
};

export default NavBar;
